<resources>
    <!-- Base application theme. -->
    <style name="Theme.LinearLayoutChallenge" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryBlack</item>
        <item name="colorPrimaryVariant">@color/secondaryBlack</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/fourthBlack</item>
        <!-- Customize your theme here. -->
    </style>
</resources>